#ifndef LAYER_HPP
#define LAYER_HPP

#include "EntityCollection.h"

//Backgroud (images), tilemap, sparse tile map, ...

class Layer
{
    public:

    Layer();
    ~Layer();

    private:

    //EntityCollection* entities;

};

#endif